name: Test and Publish Package

on:
  push:
    branches: [ main, 'release/v*' ]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      test_only:
        description: 'Run only test-jest (skip publish). Use this to validate workflow/action changes without publishing.'
        required: false
        type: boolean
        default: false
      version:
        description: 'Version to publish (leave empty to use package.json version)'
        required: false
        type: string
        default: ''
      branch:
        description: 'Branch to publish from (leave empty to use current branch). Note: GitHub UI branch selector order cannot be customized.'
        required: false
        type: string
        default: ''
      force:
        description: 'Force publish even if version already exists (use with caution)'
        required: false
        type: boolean
        default: false

jobs:
  test-jest:
    name: Jest Tests (Unit & Integration)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch || github.ref }}
      
      - name: Prelim (Node, install, build)
        uses: Signal-Meaning/dg_react_agent/.github/actions/prelim@main
        with:
          ref: ${{ inputs.branch || github.ref }}

      - name: Install voice-agent-backend dependencies
        run: cd packages/voice-agent-backend && npm ci

      - name: Run linting
        run: npm run lint
        
      - name: Run Jest tests (mock only)
        run: npm run test:mock
        env:
          CI: true
          RUN_REAL_API_TESTS: false
          NODE_ENV: test
        
      - name: Test package packaging
        run: |
          # Test that the package can be packaged for local use
          npm run package:local
          
          # Verify the package was created
          ls -la *.tgz
          
      - name: Test package installation from tarball
        run: |
          # Create a temporary directory to test package installation
          mkdir test-install
          cd test-install
          
          # Create a test package.json
          echo '{"name": "test-install", "version": "1.0.0", "dependencies": {}}' > package.json
          
          # Install from the local tarball
          npm install ../*.tgz
          
          # Verify the package was installed
          ls node_modules/@signal-meaning/voice-agent-react
          
          # Test that the package can be imported
          node -e "console.log('Testing import...'); const pkg = require('@signal-meaning/voice-agent-react'); console.log('Package imported successfully:', Object.keys(pkg));"
          
          # Clean up
          cd ..
          rm -rf test-install

      - name: Validate voice-agent-backend package (pack dry-run)
        run: |
          cd packages/voice-agent-backend
          npm pack --dry-run

  # E2E tests temporarily disabled in CI
  # test-e2e:
  #   name: Playwright E2E Tests
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #       with:
  #         ref: ${{ inputs.branch || github.ref }}
  #     
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         
  #     - name: Install dependencies
  #       run: npm ci
  #       
  #     - name: Build package
  #       run: npm run build
  #       
  #     - name: Install test-app dependencies
  #       run: |
  #         cd test-app
  #         npm ci
  #       
  #     - name: Install Playwright browsers (Chromium only)
  #       run: |
  #         cd test-app
  #         npx playwright install chromium --with-deps
  #     
  #     - name: Verify test-app can start dev server
  #       run: |
  #         cd test-app
  #         timeout 30 npm run dev &
  #         sleep 10
  #         curl -f http://localhost:5173 || (echo "❌ Dev server failed to start" && exit 1)
  #         pkill -f "vite" || true
  #         echo "✅ Dev server starts successfully"
  #       
  #     - name: Run Playwright E2E tests (CI-safe only)
  #       run: |
  #         cd test-app
  #         # Run only CI-safe E2E tests (core functionality without audio/microphone dependencies)
  #         # Use --config to ensure webServer config is loaded
  #         npx playwright test --config=tests/playwright.config.mjs \
  #           tests/e2e/api-key-validation.spec.js \
  #           tests/e2e/text-only-conversation.spec.js \
  #           tests/e2e/lazy-initialization-e2e.spec.js \
  #           tests/e2e/page-content.spec.js \
  #           tests/e2e/deepgram-ux-protocol.spec.js \
  #           tests/e2e/protocol-validation-modes.spec.js
  #       env:
  #         CI: true
  #         VITE_DEEPGRAM_API_KEY: ${{ secrets.VITE_DEEPGRAM_API_KEY }}
  #         VITE_DEEPGRAM_PROJECT_ID: ${{ secrets.VITE_DEEPGRAM_PROJECT_ID }}
  #         VITE_BASE_URL: 'http://localhost:5173'

  # Publishes two packages: @signal-meaning/voice-agent-react (root) and @signal-meaning/voice-agent-backend.
  # Requires NPM_TOKEN secret with write:packages (classic PAT). See docs/issues/ISSUE-425/PUBLISH-BACKEND-401-INVESTIGATION.md if publish fails with 401.
  publish:
    name: Publish Package
    runs-on: ubuntu-latest
    needs: [test-jest] # test-e2e temporarily disabled
    if: success() && (github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.test_only != true) || (github.event_name == 'push' && startsWith(github.ref, 'refs/heads/release/v')))
    permissions:
      contents: write   # read + write for creating release tag and GitHub Release
      packages: write
      id-token: write
      
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch || github.ref }}
      
      - name: Prelim (Node, install, build)
        uses: Signal-Meaning/dg_react_agent/.github/actions/prelim@main
        with:
          ref: ${{ inputs.branch || github.ref }}
          registry-url: 'https://npm.pkg.github.com'
          scope: '@signal-meaning'
          token: ${{ secrets.NPM_TOKEN }}
      
      - name: Verify npm configuration
        run: |
          # setup-node@v4 automatically configures authentication
          # We just verify the registry is set correctly
          echo "📋 Verifying npm configuration..."
          echo "Default registry: $(npm config get registry)"
          echo "Scope registry: $(npm config get @signal-meaning:registry)"
          
          # Verify .npmrc exists (location set by setup-node@v4)
          if [ -n "$NPM_CONFIG_USERCONFIG" ]; then
            if [ -f "$NPM_CONFIG_USERCONFIG" ]; then
              echo "✅ npmrc configuration file exists at: $NPM_CONFIG_USERCONFIG"
              echo "📄 npmrc content (first few lines, masked):"
              head -3 "$NPM_CONFIG_USERCONFIG" | sed 's/_authToken=.*/_authToken=***MASKED***/' || true
            else
              echo "⚠️ npmrc file not found at expected location: $NPM_CONFIG_USERCONFIG"
              echo "🔧 Attempting to create .npmrc manually as fallback..."
              echo "//npm.pkg.github.com/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc
              echo "@signal-meaning:registry=https://npm.pkg.github.com" >> ~/.npmrc
              echo "✅ Created ~/.npmrc as fallback"
            fi
          else
            echo "⚠️ NPM_CONFIG_USERCONFIG not set, creating .npmrc manually..."
          echo "//npm.pkg.github.com/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc
          echo "@signal-meaning:registry=https://npm.pkg.github.com" >> ~/.npmrc
            echo "✅ Created ~/.npmrc"
          fi
          
          # Also ensure project-level .npmrc exists (for compatibility)
          if [ ! -f .npmrc ]; then
            echo "@signal-meaning:registry=https://npm.pkg.github.com" > .npmrc
            echo "//npm.pkg.github.com/:_authToken=${{ secrets.NPM_TOKEN }}" >> .npmrc
            echo "✅ Created project-level .npmrc"
          fi
        
      - name: Verify authentication
        run: |
          echo "🔐 Verifying authentication..."
          npm whoami --registry https://npm.pkg.github.com || {
            echo "❌ Authentication verification failed"
            echo ""
            echo "Common causes:"
            echo "  1. NPM_TOKEN secret missing or invalid"
            echo "  2. Token lacks 'write:packages' permission"
            echo "  3. Token lacks access to @signal-meaning organization"
            echo "  4. Token expired"
            echo ""
            echo "Check repository settings:"
            echo "  - Secrets > NPM_TOKEN exists"
            echo "  - Token has write:packages scope"
            echo "  - Token has organization access"
            exit 1
          }
          echo "✅ Authentication verified"

      # --- Package 1: @signal-meaning/voice-agent-react (root) ---
      - name: Determine version (voice-agent-react)
        id: root_version
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            VERSION="${{ inputs.version }}"
          else
            VERSION=$(node -p "require('./package.json').version")
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 voice-agent-react version: $VERSION"

      - name: Check if version exists (voice-agent-react)
        id: check_root
        run: |
          V="${{ steps.root_version.outputs.version }}"
          if npm view @signal-meaning/voice-agent-react@$V --registry https://npm.pkg.github.com 2>/dev/null; then
            echo "version_exists=true" >> $GITHUB_OUTPUT
            echo "⚠️ @signal-meaning/voice-agent-react@$V already in registry"
          else
            echo "version_exists=false" >> $GITHUB_OUTPUT
            echo "✅ @signal-meaning/voice-agent-react@$V not in registry, will publish"
          fi
        continue-on-error: true

      - name: Publish (voice-agent-react)
        env:
          PACKAGE_NAME: "@signal-meaning/voice-agent-react"
          PACKAGE_DIR: "."
          PACKAGE_VERSION: ${{ steps.root_version.outputs.version }}
          VERSION_EXISTS: ${{ steps.check_root.outputs.version_exists }}
        run: |
          set -e
          if [ "$VERSION_EXISTS" == "true" ] && [ "${{ inputs.force }}" != "true" ]; then
            echo "⚠️ $PACKAGE_NAME@$PACKAGE_VERSION already in registry; skipping (use force to overwrite)."
            exit 0
          fi
          cd "$PACKAGE_DIR"
          echo "📦 Publishing $PACKAGE_NAME@$PACKAGE_VERSION from $PACKAGE_DIR"
          npm pack --dry-run 2>/dev/null | tail -15 || true
          npm publish 2>&1 | tee /tmp/npm-publish.log
          EXIT_CODE=${PIPESTATUS[0]}
          if [ -n "$EXIT_CODE" ] && [ "$EXIT_CODE" -ne 0 ]; then
            echo "❌ Publish failed (exit $EXIT_CODE)"
            cat /tmp/npm-publish.log
            echo "💡 401? Ensure NPM_TOKEN has write:packages — see docs/issues/ISSUE-425/PUBLISH-BACKEND-401-INVESTIGATION.md"
            exit $EXIT_CODE
          fi
          echo "✅ $PACKAGE_NAME published successfully"

      # --- Package 2: @signal-meaning/voice-agent-backend ---
      - name: Determine version (voice-agent-backend)
        id: backend_version
        run: |
          VERSION=$(node -p "require('./packages/voice-agent-backend/package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 voice-agent-backend version: $VERSION"

      - name: Check if version exists (voice-agent-backend)
        id: check_backend
        run: |
          V="${{ steps.backend_version.outputs.version }}"
          if npm view @signal-meaning/voice-agent-backend@$V --registry https://npm.pkg.github.com 2>/dev/null; then
            echo "version_exists=true" >> $GITHUB_OUTPUT
            echo "⚠️ @signal-meaning/voice-agent-backend@$V already in registry"
          else
            echo "version_exists=false" >> $GITHUB_OUTPUT
            echo "✅ @signal-meaning/voice-agent-backend@$V not in registry, will publish"
          fi
        continue-on-error: true

      - name: Publish (voice-agent-backend)
        env:
          PACKAGE_NAME: "@signal-meaning/voice-agent-backend"
          PACKAGE_DIR: "packages/voice-agent-backend"
          PACKAGE_VERSION: ${{ steps.backend_version.outputs.version }}
          VERSION_EXISTS: ${{ steps.check_backend.outputs.version_exists }}
        run: |
          set -e
          if [ "$VERSION_EXISTS" == "true" ] && [ "${{ inputs.force }}" != "true" ]; then
            echo "⚠️ $PACKAGE_NAME@$PACKAGE_VERSION already in registry; skipping (use force to overwrite)."
            exit 0
          fi
          cd "$PACKAGE_DIR"
          echo "📦 Publishing $PACKAGE_NAME@$PACKAGE_VERSION from $PACKAGE_DIR"
          npm pack --dry-run 2>/dev/null | tail -15 || true
          npm publish 2>&1 | tee /tmp/npm-publish.log
          EXIT_CODE=${PIPESTATUS[0]}
          if [ -n "$EXIT_CODE" ] && [ "$EXIT_CODE" -ne 0 ]; then
            echo "❌ Publish failed (exit $EXIT_CODE)"
            cat /tmp/npm-publish.log
            echo "💡 401? Ensure NPM_TOKEN has write:packages — see docs/issues/ISSUE-425/PUBLISH-BACKEND-401-INVESTIGATION.md"
            exit $EXIT_CODE
          fi
          echo "✅ $PACKAGE_NAME published successfully"

      - name: Verify package installation (voice-agent-react)
        run: |
          # Create a temporary directory to test package installation
          mkdir test-install
          cd test-install
          
          # Create a test package.json
          echo '{"name": "test-install", "version": "1.0.0", "dependencies": {}}' > package.json
          
          # Configure npm for GitHub Package Registry
          echo "@signal-meaning:registry=https://npm.pkg.github.com" > .npmrc
          echo "//npm.pkg.github.com/:_authToken=${{ secrets.NPM_TOKEN }}" >> .npmrc
          
          # Install the package
          npm install @signal-meaning/voice-agent-react
          
          # Verify the package was installed
          ls node_modules/@signal-meaning/voice-agent-react
          
          # Clean up
          cd ..
          rm -rf test-install

      - name: Verify package installation (voice-agent-backend)
        continue-on-error: true
        run: |
          mkdir test-install-backend
          cd test-install-backend
          echo '{"name": "test-install-backend", "version": "1.0.0", "dependencies": {}}' > package.json
          echo "@signal-meaning:registry=https://npm.pkg.github.com" > .npmrc
          echo "//npm.pkg.github.com/:_authToken=${{ secrets.NPM_TOKEN }}" >> .npmrc
          npm install @signal-meaning/voice-agent-backend
          ls node_modules/@signal-meaning/voice-agent-backend
          node -e "const pkg = require('@signal-meaning/voice-agent-backend'); console.log('voice-agent-backend imported:', Object.keys(pkg));"
          cd ..
          rm -rf test-install-backend

      # Create GitHub Release (and tag) when triggered by push to release/v* or workflow_dispatch (not when trigger is release:published)
      - name: Create GitHub Release
        if: success() && (github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && startsWith(github.ref, 'refs/heads/release/v')))
        run: |
          VER="v${{ steps.root_version.outputs.version }}"
          if gh release view "$VER" --repo $GITHUB_REPOSITORY 2>/dev/null; then
            echo "Release $VER already exists, skipping."
            exit 0
          fi
          NOTES="docs/releases/$VER/RELEASE-NOTES.md"
          if [ -f "$NOTES" ]; then
            gh release create "$VER" --title "$VER" --notes-file "$NOTES" --repo $GITHUB_REPOSITORY
          else
            gh release create "$VER" --title "$VER" --notes "Release $VER" --repo $GITHUB_REPOSITORY
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
